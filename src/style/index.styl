@import 'reset'

body
  font-size calc(100vw / 750)
  font-family Arial
  background-color $primary
  min-height 100%
  min-width 100%
  height 100vh
  overflow scroll
  *
    -webkit-overflow-scrolling touch

html,
body,
#app {
  height 100%
  width 100%
}
.amsg {
  z-index 1000
}